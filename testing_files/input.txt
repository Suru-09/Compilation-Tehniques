struct pt {
	int x;
	int b;
	//int x;
};

struct pt z[21];

void main() {
	int d;
	struct pt xz;
	int c, f[123];
	int e[100];
	double g;
	double hello;
	char goodbye;
	return;
}

int * mamaliga(double abc, char mamaliga2, char macaroane) {
	if(5 > 5) {
		int a;
		int w;
		int c;
		if(1) {
			double dada;
		}
	}
	else {
		double carate;
	}
	int gama[50];

	int omega;
	double k;
	omega = gama[1];
	return 3;
	omega = z[1].x;

	//!omega;
	//-omega;

	char b2[20];

	// (struct pt) omega;
	// (struct m) omega;

	omega = 1 * 2 * omega * 2.34;
	// 2 = z[5].x < 1.92e5;
	//k = z[2].x < 1.92e5;
	// k = 2 || 3 && 4 && 5 && z;
	k = 2 || 3 && 4 && 5 && z[2].x;

	k == z[0].b;
	k != z[1].b;

	if( k == "string" ) {
		return 5;
	}

	if (omega > 3 && z[1].x < 2 || z[2].b > 4.5) {
		return 3;
	}

	while ( z[3].x ) {
		return 7;
	}

	int i;
	for( i = 0 ; z[2].x ; i = i + 1 ){
		return 15;
	}

}

/*
int wtf() {
	double c;
	char d;
	int b[10];
	main();
	mamaliga(c, d, c);
}*/

/*struct z main() {
	return "not ok";
}*/